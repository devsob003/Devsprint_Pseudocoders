import { utf8 } from "@project-serum/anchor/dist/cjs/utils/bytes";

export const PROFILE_STATE = utf8.encode('PROFILE_STATE');
export const MEMBERSHIP_PLAN_STATE = utf8.encode('MEMBERSHIP_PLAN_STATE');
export const POST_STATE = utf8.encode('POST');
export const COMMENT_STATE = utf8.encode('COMMENT');
